# Experiments

Store reproducible experiment summaries here. Every probability or optimization experiment must reference its run manifest, deck version, code revision, and raw artifact location.

Do not commit large generated datasets or simulation outputs unless the checkpoint explicitly requires a small golden fixture.
